make  PREFIX=${INSTALL_ROOT}/usr                       \
      BINDIR=${INSTALL_ROOT}/usr/games                 \
      -f  makefile.unix                                \
      install                                          &&
cp  -vr  ctrlr  ${INSTALL_DIR}/usr/share/games/xmess   &&
cd ${INSTALL_DIR}/usr/share/games/xmess                &&
find_and_scam
