cd $SOURCE_DIRECTORY  &&
install -m755 bin/chromium ${INSTALL_ROOT}/usr/share/games/chromium/bin/chromium &&    
cd ${INSTALL_ROOT}/usr/share/games/chromium/ &&
find_and_scam &&
cp ${SCRIPT_DIRECTORY}/chromium ${INSTALL_ROOT}/usr/games/chromium &&
chmod +x ${INSTALL_ROOT}/usr/games/chromium &&
chown root:games ${INSTALL_ROOT}/usr/games/chromium
