cd /tmp &&
mkdir -p /tmp/wormux-v060beta1 &&
cd /tmp/wormux-v060beta1 &&
strace -o /tmp/cvs.log cvs -q \
    -z3 \
    -d:pserver:anonymous@cvs.gna.org:/cvs/wormux \
    checkout \
    -r v60beta1 \
    wormux &&
cd /tmp &&
tar -jvcf /var/spool/sorcery/wormux-v060beta1.tar.bz2 \
    wormux-v060beta1
