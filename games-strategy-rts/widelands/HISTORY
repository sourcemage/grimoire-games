2007-09-14 Jaka Kranjc <lynxlynxlynx@sourcemage.org>
	* BUILD: make sure the GGZ swapping gets run even if BUILD fails
	  match scons loosely, not sure if the internal is still needed though
	  added make as a fallback, otherwise it breaks here, similar to b10
	* DETAILS: fixed WEB_SITE
	  updated spell to build11

2007-04-25 Andraž "ruskie" Levstik <ruskie@mages.ath.cx>
	* BUILD: uses scons now
	* CONFIGURE: add more build targets
	* DEPENDS: cvs -> svn, some depends updates
	* DETAILS: version update and cvs -> svn change
	* INSTALL: uses scons now
	* PREPARE: changed to svn
	* PRE_BUILD: modified to work with scons and our CXXFLAGS
	* widelands: removed no longer needed

2006-09-21 Andraž "ruskie" Levstik <ruskie@mages.ath.cx>
	* DETAILS: [automated] Removed BUILD_API=2.

2005-12-10 Robin Cook <rcook@wyrms.net>
	* DETAILS: added release b9half and cvs
	  corrected WEB_SITE
	* PREPARE: added to ask cvs query
	* CONFIGURE: added to query build type and ggz support
	* DEPENDS: set depends CVS only if using cvs
	  added depends on sds_net, python, gettext, libpng, zlib, doxygenC
	  set ggz depends depending on the query
	* PRE_BUILD: remove CVS directories
	* INSTALL: add additional directories
	  Add lines to Makefile.local depending on queries

2005-12-01 Seth Woolley <seth@tautology.org>
	* DETAILS: MD5 IGNORE to SOURCE_HASH unversioned

2004-09-25 Arwed v. Merkatz <v.merkatz@gmx.net>
	* BUILD, DEPENDS, DETAILS, HISTORY, INSTALL, widelands:
	  created the spell
