config_query WIDELANDS_SCM "Download latest scm code?" ${WIDELANDS_CVS:-n}  &&

if [[ "$WIDELANDS_SCM" == "y" ]]; then
   config_query WIDELANDS_SCM_AUTOUPDATE \
                "Update to the latest revision on every cast?" ${WIDELANDS_CVS_AUTOUPDATE:-n}
fi

