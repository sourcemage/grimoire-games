default_pre_build  &&
cd ${SOURCE_DIRECTORY}  &&

# disabling compilation of BulletMultiThreaded on x86_64
if [[ ${SMGL_COMPAT_ARCHS[1]} == "x86_64" ]]; then
        patch -p1 < ${SPELL_DIRECTORY}/bullet-multithreaded-disable.patch
fi
