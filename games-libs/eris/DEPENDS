depends  libsigc++2  &&

if  [  "$CVS"  ==  "y"  ];  then
  message  "${SPELL_COLOR}eris${MESSAGE_COLOR CVS requires ${SPELL_COLOR}atlas-c++${MESSAGE_COLOR} CVS"  &&
  depends  skstream  &&
  depends  wfmath    &&
  depends  cvs
else
  depends  skstream2  &&
  depends  wfmath2
fi
