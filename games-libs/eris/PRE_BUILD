default_pre_build                          &&
cd  $SOURCE_DIRECTORY                      &&
patch -p0 < $SCRIPT_DIRECTORY/gcc43.patch  &&

if  [  "$ERIS_BRANCH"  ==  "cvs"  ];  then
  NOCONFIGURE="on"  ./autogen.sh
fi
