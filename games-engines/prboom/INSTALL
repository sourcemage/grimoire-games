default_install                            &&
mv  /usr/games/prboom  ${I_DIR}            &&
${INSTBIN}  ${SCRIPT_DIRECTORY}/prboom     \
            /usr/games                     &&
chown  -R  root.games  ${I_DIR}
