diff -urpN entityforge-0.2.old/configure entityforge-0.2/configure
--- entityforge-0.2.old/configure	2004-10-09 14:08:14.163948408 -0700
+++ entityforge-0.2/configure	2004-10-09 14:09:11.838180592 -0700
@@ -10951,23 +10951,23 @@ fi
   else
      PKG_CONFIG_MIN_VERSION=0.9.0
      if $PKG_CONFIG --atleast-pkgconfig-version $PKG_CONFIG_MIN_VERSION; then
-        echo "$as_me:$LINENO: checking for gtkglextmm-1.0 >= 1.00" >&5
-echo $ECHO_N "checking for gtkglextmm-1.0 >= 1.00... $ECHO_C" >&6
+        echo "$as_me:$LINENO: checking for gtkglextmm-1.1 >= 1.1.0" >&5
+echo $ECHO_N "checking for gtkglextmm-1.2 >= 1.1.0... $ECHO_C" >&6
 
-        if $PKG_CONFIG --exists "gtkglextmm-1.0 >= 1.00" ; then
+        if $PKG_CONFIG --exists "gtkglextmm-1.2 >= 1.1.0" ; then
             echo "$as_me:$LINENO: result: yes" >&5
 echo "${ECHO_T}yes" >&6
             succeeded=yes
 
             echo "$as_me:$LINENO: checking GTKGLEXTMM_CFLAGS" >&5
 echo $ECHO_N "checking GTKGLEXTMM_CFLAGS... $ECHO_C" >&6
-            GTKGLEXTMM_CFLAGS=`$PKG_CONFIG --cflags "gtkglextmm-1.0 >= 1.00"`
+            GTKGLEXTMM_CFLAGS=`$PKG_CONFIG --cflags "gtkglextmm-1.2 >= 1.1.0"`
             echo "$as_me:$LINENO: result: $GTKGLEXTMM_CFLAGS" >&5
 echo "${ECHO_T}$GTKGLEXTMM_CFLAGS" >&6
 
             echo "$as_me:$LINENO: checking GTKGLEXTMM_LIBS" >&5
 echo $ECHO_N "checking GTKGLEXTMM_LIBS... $ECHO_C" >&6
-            GTKGLEXTMM_LIBS=`$PKG_CONFIG --libs "gtkglextmm-1.0 >= 1.00"`
+            GTKGLEXTMM_LIBS=`$PKG_CONFIG --libs "gtkglextmm-1.2 >= 1.1.0"`
             echo "$as_me:$LINENO: result: $GTKGLEXTMM_LIBS" >&5
 echo "${ECHO_T}$GTKGLEXTMM_LIBS" >&6
         else
@@ -10975,7 +10975,7 @@ echo "${ECHO_T}$GTKGLEXTMM_LIBS" >&6
             GTKGLEXTMM_LIBS=""
             ## If we have a custom action on failure, don't print errors, but
             ## do set a variable so people can do so.
-            GTKGLEXTMM_PKG_ERRORS=`$PKG_CONFIG --errors-to-stdout --print-errors "gtkglextmm-1.0 >= 1.00"`
+            GTKGLEXTMM_PKG_ERRORS=`$PKG_CONFIG --errors-to-stdout --print-errors "gtkglextmm-1.2 >= 1.1.0"`
             echo $GTKGLEXTMM_PKG_ERRORS
         fi
 
@@ -10990,8 +10990,8 @@ echo "${ECHO_T}$GTKGLEXTMM_LIBS" >&6
   if test $succeeded = yes; then
      :
   else
-     { { echo "$as_me:$LINENO: error: Library requirements (gtkglextmm-1.0 >= 1.00) not met; consider adjusting the PKG_CONFIG_PATH environment variable if your libraries are in a nonstandard prefix so pkg-config can find them." >&5
-echo "$as_me: error: Library requirements (gtkglextmm-1.0 >= 1.00) not met; consider adjusting the PKG_CONFIG_PATH environment variable if your libraries are in a nonstandard prefix so pkg-config can find them." >&2;}
+     { { echo "$as_me:$LINENO: error: Library requirements (gtkglextmm-1.2 >= 1.1.0) not met; consider adjusting the PKG_CONFIG_PATH environment variable if your libraries are in a nonstandard prefix so pkg-config can find them." >&5
+echo "$as_me: error: Library requirements (gtkglextmm-1.2 >= 1.1.0) not met; consider adjusting the PKG_CONFIG_PATH environment variable if your libraries are in a nonstandard prefix so pkg-config can find them." >&2;}
    { (exit 1); exit 1; }; }
   fi
 
diff -urpN entityforge-0.2.old/configure.ac entityforge-0.2/configure.ac
--- entityforge-0.2.old/configure.ac	2004-10-09 14:08:14.166947952 -0700
+++ entityforge-0.2/configure.ac	2004-10-09 14:09:18.153220560 -0700
@@ -17,7 +17,7 @@ PKG_CHECK_MODULES(GTKMM,[gtkmm-2.4 >= 2.
 AC_SUBST(GTKMM_CFLAGS)
 AC_SUBST(GTKMM_LIBS)
 
-PKG_CHECK_MODULES(GTKGLEXTMM,[gtkglextmm-1.0 >= 1.00])
+PKG_CHECK_MODULES(GTKGLEXTMM,[gtkglextmm-1.2 >= 1.1.0])
 AC_SUBST(GTKGLEXTMM_CFLAGS)
 AC_SUBST(GTKGLEXTMM_LIBS)
 
