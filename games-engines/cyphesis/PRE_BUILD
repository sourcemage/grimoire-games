default_pre_build                          &&
cd $SOURCE_DIRECTORY                       &&
patch -p1 < $SCRIPT_DIRECTORY/gcc43.patch  &&

if  [  "$CYPHESIS_CVS"  ==  "y"  ];  then
  NOCONFIGURE="ON"  ./autogen.sh
fi
