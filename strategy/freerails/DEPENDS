if grep -q "with-paragui" ${SPELL_CONFIG}
then depends SDL &&
     depends SDL_ttf &&
     depends paragui "--disable-paraguitest" &&
     depends freetype1	
fi &&
if grep -q "enable-qt" ${SPELL_CONFIG}
then depends qt-x11
fi
