default_pre_build            &&
cd  $SOURCE_DIRECTORY        &&
mkdir -p content             &&
cd   content                 &&
unpack  $SOURCE2  ${MD5[1]}  &&

if  [  "$SC2_AUDIO"  ==  "y"  ];  then
  unpack  $SOURCE3  ${MD5[2]}  &&
  unpack  $SOURCE4  ${MD5[3]}
fi
