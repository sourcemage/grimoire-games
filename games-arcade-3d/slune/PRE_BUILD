default_pre_build                                             &&
cd $SOURCE_DIRECTORY                                          &&
sedit "s=usr/share=$INSTALL_ROOT/usr/share/games=" setup.cfg  &&
sedit "s=usr/bin=$INSTALL_ROOT/usr/games=" setup.cfg 
