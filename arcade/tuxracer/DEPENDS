depends  GLUT       &&
depends  tcl        &&
depends  SDL        &&
depends  SDL_mixer  &&

optional_depends  nvidia_headers  \
                  ""              \
                  ""              \
                  "for NVIDIA specific extensions to OpenGL (suitable ONLY for NVIDIA cards)"
