default_install                                      &&
cd ${SOURCE_DIRECTORY}/sandr-data                    &&
find_and_scam                                        &&
mkdir -vp ${INSTALL_ROOT}/usr/share/games/${PKG}/    &&
cp -var * ${INSTALL_ROOT}/usr/share/games/${PKG}/
