depends  simgear	&&
depends  fgfs-base	&&

if [[ "${GRAPHIC_LIB}" == "sdl" ]]
 then
 depends sdl "--enable-sdl"
 else
  if [[ "${GRAPHIC_LIB}" == "openglut" ]]
   then
   depends openglut
   else
   depends glut
  fi
fi
