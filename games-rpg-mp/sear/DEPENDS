if  [  "$SEAR_CVS"  ==  "y"  ];  then
  depends  libmodelfile  &&
  message  "${MESSAGE_COLOR}Depends on the devel branch of"  \
           "${SPELL_COLOR}atlas-c++${DEFAULT_COLOR}"         &&
  depends  atlas-c++     &&
  depends  guichan       &&
  message  "${MESSAGE_COLOR}Depends on the devel branch of"  \
           "${SPELL_COLOR}eris${DEFAULT_COLOR}"              &&
  depends  eris
else
  depends  eris-transitional
fi  &&

depends  sear-media         &&
depends  GLUT               &&
depends  mercator           &&
depends  sdl_image          &&
depends  sdl_mixer          &&
depends  varconf2           &&
depends  cal3d              &&
depends  sage               &&
depends  lib3ds
