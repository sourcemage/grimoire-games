default_pre_build &&

sedit 's:^#define CONFIG_DATA.*$:#define CONFIG_DATA \"/usr/share/neverball/data\":' share/config.h &&
sedit 's/#X11_PATH/X11_PATH/' Makefile &&

case  $CUSTOM in
  y|Y)  ${EDITOR:-nano}  share/config.h  ;;
esac
