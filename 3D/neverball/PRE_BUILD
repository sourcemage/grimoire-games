default_pre_build &&

sedit 's:^#define CONFIG_DATA.*$:#define CONFIG_DATA \"/usr/share/neverball/data\":' $SOURCE_DIRECTORY/share/config.h &&
sedit 's/#X11_PATH/X11_PATH/' $SOURCE_DIRECTORY/Makefile &&

case  $CUSTOM in
  y|Y)  ${EDITOR:-nano}  $SOURCE_DIRECTORY/share/config.h  ;;
esac
