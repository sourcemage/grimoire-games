(

  ./configure  --prefix=${INSTALL_ROOT}/usr                  \
               --bindir=${INSTALL_ROOT}/usr/games            &&
  make                                        &&
  prepare_install                             &&
  make    install

) > $C_FIFO 2>&1
